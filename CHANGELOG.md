# 更新说明

## 1.2.0

- 增加原生 Anthropic Messages API：`/v1/messages` 与 `/v1/messages/count_tokens`，兼容 Claude Code、CC Switch。
- 增强多账号池：记录错误次数、冷却截止时间、冷却类型、最近错误、余额和同步时间；对 Token 失效、账号不存在、限流、服务错误和网络错误自动冷却。
- 增加上游审计头：`X-Agent-Purpose`、`X-IDE-Name`、`X-IDE-Type`、`X-Product` 及客户端 IP 头。
- 用量记录增加客户端 IP、TTFB、错误类型、估算积分和真实积分字段。
- API Key 支持积分额度与超额拦截，返回标准 `402 quota_exceeded`。
- 支持按模型倍率估算积分，并提供从上游模型目录同步真实倍率的按钮。
- 增加延迟积分校正：约 60 秒后查询账号余额，写回实际积分并修正 Key 用量。
- 账号余额查询会回写总量、剩余量和最近同步时间。
- 保留国内/国际区域隔离、SQLite 自动迁移、本机默认监听和 Vue 管理页。

> 真实倍率依赖上游账号可访问模型目录；上游未返回倍率的模型会显示“未同步”，不会伪装成固定倍率。
